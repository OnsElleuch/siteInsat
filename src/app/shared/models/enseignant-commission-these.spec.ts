import { EnseignantCommissionThese } from './enseignant-commission-these';

describe('EnseignantCommissionThese', () => {
  it('should create an instance', () => {
    expect(new EnseignantCommissionThese()).toBeTruthy();
  });
});
